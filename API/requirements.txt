# ============================================================
# Dependências do Mnemo (ponto de entrada único para a imagem
# de produção).
#
#   requirements-base.txt → API + testes (usado pelo CI)
#   requirements-ml.txt   → dependências pesadas (Whisper/torch)
#
# O CI instala apenas a base; a imagem Docker instala este
# requirements.txt (base + ml) depois de instalar o torch do
# índice CPU no Dockerfile.
# ============================================================

-r requirements-base.txt
-r requirements-ml.txt